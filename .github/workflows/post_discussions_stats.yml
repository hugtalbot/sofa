name: Post GitHub Discussions Statistics

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * 1' # Every Monday at 7:00 AM UTC (9:00 AM CET)
  pull_request:
    types:
      - opened
      - synchronize
      - labeled
      - unlabeled

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10' # Use the desired Python version

      - name: Install dependencies
        run: |
          pip install python-graphql-client
          pip install python-dateutil
        working-directory: ${{ github.workspace }}

      - name: Run script get-weekly-discussions-stats.py
        id: create_message
        run: |
          echo "::set-output name=output_var::$(python scripts/get-weekly-discussions-stats.py)"
        working-directory: ${{ github.workspace }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Discord Webhook Action
        uses: tsickert/discord-webhook@v5.3.0
        with:
          webhook-url: 'https://discord.com/api/webhooks/1196926587615059988/4I5uKA5ItYKefKrYWRBuoCU7KPVYtmGEKApwoU4xwEAohUebQ66y-Q-r7RKW-XnzgfI6'
          content: ${{ steps.create_message.outputs.output_var }}

